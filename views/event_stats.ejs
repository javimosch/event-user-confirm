<!DOCTYPE html>
<html lang="<%= currentLocale %>">
<head>
  <meta charset="UTF-8">
  <title><%= process.env.PAGE_TITLE || 'EUC' %></title>
  <link rel="icon" type="image/x-icon" href="/favicon.ico?v=2">
  <link href="/css/output.css" rel="stylesheet">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body class="bg-gray-100 p-4">
  <div class="max-w-2xl mx-auto">
    <h1 class="text-2xl font-bold mb-6"><%= event.name %> - <%= __('stats.title') %></h1>
    
    <div class="bg-white shadow rounded p-6 space-y-6">
      <div class="grid grid-cols-2 gap-4">
        <div class="p-4 bg-green-50 rounded border border-green-200">
          <h2 class="text-lg font-semibold text-green-700 mb-2"><%= __('stats.going.title') %></h2>
          <div class="text-3xl font-bold text-green-600"><%= stats.going %></div>
          <% if(event.allowGuests && stats.totalGuests > 0) { %>
            <div class="text-sm text-green-600 mt-1">+<%= stats.totalGuests %> <%= __('stats.going.additionalGuests') %></div>
          <% } %>
        </div>
        
        <div class="p-4 bg-red-50 rounded border border-red-200">
          <h2 class="text-lg font-semibold text-red-700 mb-2"><%= __('stats.notGoing.title') %></h2>
          <div class="text-3xl font-bold text-red-600"><%= stats.notGoing %></div>
        </div>
      </div>
      
      <div class="p-4 bg-blue-50 rounded border border-blue-200">
        <h2 class="text-lg font-semibold text-blue-700 mb-2"><%= __('stats.totalAttending.title') %></h2>
        <div class="text-3xl font-bold text-blue-600">
          <%= stats.totalAttending %>
          <span class="text-lg font-normal"><%= __('stats.totalAttending.people') %></span>
        </div>
      </div>
      
      <div class="p-4 bg-gray-50 rounded border border-gray-200">
        <h2 class="text-lg font-semibold text-gray-700 mb-2"><%= __('stats.responseRate.title') %></h2>
        <div class="text-3xl font-bold text-gray-600">
          <%= stats.responseRate %>%
        </div>
        <div class="text-sm text-gray-600 mt-1">
          <%= stats.totalResponses %> <%= __('stats.responseRate.totalResponses') %>
        </div>
      </div>
    </div>
    
    <div class="mt-6 text-center">
      <a href="/event/<%= event.slug %>/yes" class="inline-block px-4 py-2 bg-green-500 text-white rounded mr-2 hover:bg-green-600">
        <%= __('stats.actions.confirmGoing') %>
      </a>
      <a href="/event/<%= event.slug %>/no" class="inline-block px-4 py-2 bg-red-500 text-white rounded hover:bg-red-600">
        <%= __('stats.actions.confirmNotGoing') %>
      </a>
    </div>
  </div>
  
  <%- include('partials/footer') %>
</body>
</html>
